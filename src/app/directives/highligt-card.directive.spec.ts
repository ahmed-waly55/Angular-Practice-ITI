import { HighligtCardDirective } from './highligt-card.directive';

describe('HighligtCardDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligtCardDirective();
    expect(directive).toBeTruthy();
  });
});
